version: "3.8"
services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    env_file: ./server/.env
    restart: unless-stopped

  client:
    build: ./client
    ports:
      - "3000:3000"
    env_file: ./client/.env
    depends_on:
      - server
    restart: unless-stopped
